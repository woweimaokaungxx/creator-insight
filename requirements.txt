fastapi>=0.140
uvicorn>=0.30
httpx>=0.27
pydantic>=2.8
python-dotenv>=1.0
yt-dlp>=2024.8.6
# 抖音自动生成 cookie 需 playwright + 系统 Chrome/Edge（无需下载 playwright 自带浏览器）
playwright>=1.40
# 可选：本地 Whisper 兜底（体积较大，按需安装）
# faster-whisper>=1.0
# 可选：Whisper GPU 加速（NVIDIA 显卡；无需系统级 CUDA Toolkit，pip 装运行时库即可）
# 启用：config.json 的 transcription.whisper.device=cuda、compute_type=float16
# 程序会自动把 site-packages/nvidia/*/bin 注入 DLL 搜索路径（无需手工配 PATH）
# nvidia-cublas-cu12
# nvidia-cudnn-cu12
# nvidia-cuda-runtime-cu12
# 可选：财经数据（V0.2 验证引擎）
# akshare>=1.14
# yfinance>=0.2.40
# 可选：本地语义检索（模型可选 + 自动下载，参考 douyin-creator-distill）
# 下载仅需下面两个轻量包；真正向量化还需 sentence-transformers + torch（CPU 版）。
# modelscope>=1.39
# huggingface-hub>=0.30
# sentence-transformers>=3.0
# torch（CPU 版，按需安装：pip install torch --index-url https://download.pytorch.org/whl/cpu）
